package layouts

templ ChatLayout(args RootArgs) {
	<!DOCTYPE html>
	<html lang="en">
		@Head(args)
		<body class="bg-background font-sans antialiased overflow-hidden overscroll-none" data-signals="{theme: initTheme()}">
			<div class="h-[100svh] flex flex-col overflow-hidden bg-background">
				@Header(args)
				{ children... }
			</div>
		</body>
	</html>
}

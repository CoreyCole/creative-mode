services:
  app:
    build:
      context: .
      target: production
    ports:
      - "80:3000"
    restart: on-failure
    networks:
      - site-network
networks:
  site-network:
    driver: bridge

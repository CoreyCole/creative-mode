---
ticket: CRE-8-2
phase: research
result: success
session: 6c80a6e1
workflow: 441c51bd
timestamp: 2026-03-03T00:35:55Z
---

## BLUF
Research complete for HTTP hardening ticket. Three changes in 3 files: add Echo built-in BodyLimit + SecureWithConfig middleware, move swarm dashboard mutation routes behind AdminMiddleware, and add auth failure logging with source IPs to all three auth middlewares.

## What Was Done
- Read and analyzed all relevant source files: server.go, president_api.go, mayor_api.go, swarm_dashboard.go, swarm_api.go, auth/middleware.go
- Confirmed Echo v4 has built-in `middleware.BodyLimit()` and `middleware.SecureWithConfig()` — no new dependencies needed
- Identified that X-Frame-Options must be SAMEORIGIN (not DENY as plan suggests) because game iframes load from same origin
- Mapped all auth middleware locations and determined logging approach: pass `*slog.Logger` to hookSecretMiddleware and presidentAuthMiddleware, use existing `s.Logger` for mayorAuthMiddleware
- Verified admin gating approach: create admin sub-group for 3 swarm dashboard POST routes while keeping GET routes in approved group
- Wrote comprehensive research document at `thoughts/swarm/research/2026-03-03_00-35-55_CRE-8-2_http-hardening.md`

## What Was NOT Done
- No Linear comment posted (child ticket format CRE-8-2 may not have a direct Linear ticket)
- No implementation — this is research phase only

## Key Files
- `harness/internal/server/server.go` — Main file: add middleware, move swarm admin routes, update hookSecretMiddleware signature
- `harness/internal/server/president_api.go` — Update presidentAuthMiddleware signature, add auth failure logging
- `harness/internal/server/mayor_api.go` — Add auth failure logging to mayorAuthMiddleware
- `harness/internal/auth/middleware.go` — Reference for AdminMiddleware pattern (no changes needed)

## Gotchas
- **X-Frame-Options: Use SAMEORIGIN not DENY** — DENY breaks game iframes loaded within the harness UI. This deviates from the project plan.
- **HSTS in dev mode**: Should skip or shorten HSTS max-age when DEV_MODE=true to avoid sticky HTTPS redirects.
- **hookSecretMiddleware is called 4 times**: Changing its signature to accept a logger requires updating 4 call sites in RegisterRoutes.
- **Admin-gated buttons still visible**: After backend gating, non-admin users will see swarm approve/reject/cancel buttons that return 403. Frontend change to hide them is out of scope.

## Next Steps
- Code planning phase: create implementation plan for the 3-file change
- Key implementation decisions are resolved — straightforward middleware additions and route moves
